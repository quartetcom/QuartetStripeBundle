framework:
  secret: secret
  translator: ~

monolog:
  handlers:
    file_log:
      type: stream
      path: "%kernel.logs_dir%/test.log"

quartet_stripe:
  api_secret: stripe api secret
  api_public: stripe api public
  logger: logger
  debug: true

